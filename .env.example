# CodeRefine configuration — copy this file to `.env` and fill in your key.
# The real `.env` is gitignored and will NEVER be committed.

GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.5-flash
